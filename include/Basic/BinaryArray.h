class BinaryArray
{
public:
    // add two binary number, the number store in the array
    bool static Add(unsigned a[], unsigned b[], int n, unsigned aplusb[]);

    //print out the array
    void static PrintOut(unsigned a[], int n);
};